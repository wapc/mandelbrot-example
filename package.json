{
  "name": "browser-demo",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "asc assembly/index.ts -b wasm/mandelbrot-as.wasm --use abort=assembly/index/abort --optimize",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@wapc/as-msgpack": "^0.1.7",
    "wapc-guest-as": "git+https://github.com/wapc/wapc-guest-as#v0.2.1"
  },
  "devDependencies": {
    "assemblyscript": "^0.17.0"
  }
}
